<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuanjin.androidstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".UI.shimmer.ShimmerActivity" />
        <activity android:name=".UI.rollingnumber.RollingNumActivity" />
        <activity android:name=".KtolinSampleActivity" />
        <activity android:name=".UI.explosianimation.ExplosionAnimatorActivity">
        </activity>
        <activity android:name=".UI.calendarview2_hotel.CalendarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>